extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 Caleb Waldner | Projects

      p.lead.text-light
        | Thank you for visiting. Please browse the site and enjoy my work. Coding is a passion of mine and I hope each project demonstrates this in it's own way. 
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      //- For each project, make an anchor element with thumbnail
      each project in projectsData.projects
        .cell
          a(href=`/projects${project.id}`)
            img.thumbnail.project-thumbnail(src=`/static/images/${project.thumbnail}`)
            h5= project.project_name
      
